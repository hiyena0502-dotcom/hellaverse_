(()=>{
if(window.__HELLAVERSE_GIFT_CONTEXT_EDITOR_V1__)return;
window.__HELLAVERSE_GIFT_CONTEXT_EDITOR_V1__=1;
const K='hellaverse_dialogue_state_v1';
const ORDER=['DIRECT','DELIVERY','FOUND','PRANK','JUNK'];
const LABELS={DIRECT:'직접 고름',DELIVERY:'대신 전달',FOUND:'주운 물건',PRANK:'장난',JUNK:'쓸모없는 물건'};
const $=(s,r=document)=>r.querySelector(s);
const esc=(v='')=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function read(){try{return JSON.parse(localStorage.getItem(K)||'{}')||{}}catch{return{}}}
function write(s){localStorage.setItem(K,JSON.stringify(s));window.dispatchEvent(new CustomEvent('hellaverse:state-updated',{detail:{source:'gift-context-editor',clearDirty:false}}))}
function dataFor(state,id){let x=(state.giftContextConfig||{})[id]||{};return{enabled:x.enabled!==false,allowed:Array.isArray(x.allowed)&&x.allowed.length?x.allowed:ORDER.slice(),contextDeltas:x.contextDeltas||{},contextLines:x.contextLines||{}}}
function patch(){let card=$('#gName')?.closest('.editor-card');if(!card||card.dataset.giftContextEditor==='1')return;let state=read(),id=state.draft?.gift||'',d=dataFor(state,id),anchor=$('.gift-source-editor',card)||$('[data-gift-affection-preview]',card);if(!anchor)return;card.dataset.giftContextEditor='1';anchor.insertAdjacentHTML('afterend',`<details class="sub-editor gift-context-editor"><summary>PLAYER INTENT / DELIVERY CONTEXT</summary><p class="muted">선물을 선택한 뒤 플레이어가 왜 이 물건을 주는지 고르게 합니다. 같은 선물도 직접 고른 것인지, 누가 전해달라고 한 것인지, 주운 것인지, 장난인지에 따라 반응과 Heart를 다르게 만들 수 있습니다.</p><label class="checkline"><input id="gContextEnabled" type="checkbox" ${d.enabled?'checked':''}> 의도 선택창 사용</label><div class="gift-context-editor-allow">${ORDER.map(k=>`<label><input type="checkbox" data-context-allow="${k}" ${d.allowed.includes(k)?'checked':''}><span>${esc(k)}<small>${esc(LABELS[k])}</small></span></label>`).join('')}</div><div class="gift-context-editor-delta">${ORDER.map(k=>`<label>${esc(k)} extra Heart<input type="number" data-context-delta="${k}" value="${d.contextDeltas[k]??''}" placeholder="AUTO"></label>`).join('')}</div><details class="gift-context-lines"><summary>CUSTOM LINES</summary><p class="muted">비워두면 루시퍼의 Heart, 출처, 태그를 기준으로 자동 반응을 사용합니다.</p>${ORDER.map(k=>`<label>${esc(LABELS[k])}<textarea rows="2" data-context-line="${k}" placeholder="자동 대사를 사용하려면 비워두세요">${esc(d.contextLines[k]||'')}</textarea></label>`).join('')}</details></details>`)}
function collect(){let card=$('#gName')?.closest('.editor-card');if(!card)return null;let allowed=ORDER.filter(k=>$(`[data-context-allow="${k}"]`,card)?.checked),contextDeltas={},contextLines={};for(const k of ORDER){let dv=String($(`[data-context-delta="${k}"]`,card)?.value||'').trim(),lv=String($(`[data-context-line="${k}"]`,card)?.value||'').trim();if(dv!==''&&Number.isFinite(Number(dv)))contextDeltas[k]=Number(dv);if(lv)contextLines[k]=lv}return{enabled:!!$('#gContextEnabled',card)?.checked,allowed:allowed.length?allowed:['DIRECT'],contextDeltas,contextLines}}
let pending=null;
document.addEventListener('click',e=>{let t=e.target;if(!(t instanceof Element))return;if(t.closest('[data-save-gift]')){pending=collect();if(!pending)return;setTimeout(()=>{let state=read(),id=state.draft?.gift;if(!id)return;state.giftContextConfig=state.giftContextConfig||{};state.giftContextConfig[id]=pending;pending=null;write(state)},30)}},true);
let q=false;function schedule(){if(q)return;q=true;requestAnimationFrame(()=>{q=false;patch()})}function observe(){let app=$('#app');if(app&&!app.dataset.giftContextEditorV1){app.dataset.giftContextEditorV1='1';new MutationObserver(schedule).observe(app,{childList:true,subtree:true})}}
document.addEventListener('DOMContentLoaded',()=>{observe();patch()});window.addEventListener('load',()=>{observe();schedule()});window.addEventListener('hellaverse:state-updated',schedule);setTimeout(()=>{observe();schedule()},220);
})();